import { presets, usageFields, type Usage } from '../services/assessments';
export function UsageForm({ value, onChange, disabled = false }: { value: Usage; onChange: (v: Usage) => void; disabled?: boolean }) {
 return <fieldset disabled={disabled} className="og-usage"><legend>项目用途（可跳过，未选项保持未知）</legend><label className="og-field">用途场景<select value={value.preset} onChange={e => onChange({ ...value, preset: e.target.value as Usage['preset'] })}>{Object.entries(presets).map(([k, v]) => <option key={k} value={k}>{v}</option>)}</select></label><p className="og-muted">场景名称不代表授权结论。以下内容仅按你的明确选择记录，不会自动推断。</p><details><summary>补充用途细节（选填）</summary><div className="og-usage-grid">{Object.entries(usageFields).map(([k, label]) => <label className="og-field" key={k}>{label}<select value={value[k as keyof typeof usageFields] == null ? 'unknown' : String(value[k as keyof typeof usageFields])} onChange={e => onChange({ ...value, [k]: e.target.value === 'unknown' ? null : e.target.value === 'true' })}><option value="unknown">尚未确认</option><option value="true">是</option><option value="false">否</option></select></label>)}</div></details></fieldset>;
}
