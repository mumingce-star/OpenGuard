# AI 辅助开发记录

竞赛要求披露生成式 AI、智能体或自动化工具在代码、内容和报告中的用途，以及团队的审核、修改和验证工作。本文件从项目第一天开始维护。

| 日期 | 模型/工具 | 用途与输入范围 | 产生内容 | 团队修改 | 验证方式 | 是否纳入作品 |
|---|---|---|---|---|---|---|
| 2026-08-31 | GPT-5.6 Sol | 竞赛规则、架构和评分拆解 | 项目总体框架初稿 | 团队后续确认范围 | 对照正式附件逐项核对 | 是 |
| 2026-09-01 | GPT-5.6 Sol / Codex Root | 解析技术执行书并与正式材料、既有架构交叉核对 | P0 领域与 API 契约 v0.1.0 | 项目负责人需审核公共字段；Terra/Luna分别做实现与独立验证 | Markdown/敏感信息检查，后续以 Pydantic、JSON Schema 和负面 fixture 交叉验证 | 是 |
| 2026-09-01 | GPT-5.6 Terra | 仅使用冻结的 P0 契约 v0.1.0 实现领域模型、Schema、样例和单测 | Pydantic v2 领域模型、导出 JSON Schema、合成扫描样例与 11 项聚焦测试 | 未改变公共字段、枚举、风险语义或契约；AI 建议/候选保持待核验 | Pydantic、独立 JSON Schema、pytest、差异与敏感信息检查 | 是 |
| 2026-09-01 | GPT-5.6 Terra / Codex Root | 按冻结契约实现 A1 Pydantic 模型、Schema、样例和负面测试 | `backend/app/domain`、导出 Schema、sample 与 11 个单元测试 | Root 在隔离环境复核依赖与测试；Luna 后续独立审计边界 fixture | pytest 11/11、Pydantic/JSON Schema 双验证、Schema 导出一致性、敏感信息与 diff 检查 | 是 |
| 2026-09-01 | GPT-5.6 Luna | 独立审计 A1 P0 领域契约边界，生成聚焦测试、最小公开 fixture 和复现说明 | 跨对象引用、partial/error、locator、错误脱敏、AI pending、summary、终态时间、未知字段及公开材料检查 | 未修改公共契约或 backend；保留两项稳定实现失败，待 Terra 修复并由 Luna 回归 | pytest 33 项中 31 通过、2 项实现缺陷失败；sample Pydantic/JSON Schema 双验证与模型导出一致性通过；公开 fixture 敏感信息检查通过 | 是 |
| 2026-09-01 | GPT-5.6 Luna | 独立复核 Terra A1-fix1，验证两项运行时修订未改变公共 Schema | partial 必须含 recoverable error；Unix/Windows/UNC 路径拒绝；HTTPS 正常文本保留；Schema 导出一致和 fixture 去敏复核 | 未修改 backend、公共契约或测试预期；AI ProducerRef 字段缺口仍待 Sol 冻结 | 全量 pytest 33/33；独立 Pydantic/JSON Schema、路径/HTTPS 非回归、`git diff --check` 和公开 fixture 扫描均通过 | 是 |
| 2026-09-01 | GPT-5.6 Terra | 基于 Luna 的两项稳定 A1 边界失败，修复冻结 P0 模型的运行时语义校验 | `partial` 需包含可恢复结构化错误；错误消息拒绝嵌入式 Unix/Windows 绝对路径与凭据片段 | 仅修改 `backend/app/domain/models.py` 的内部验证逻辑；未变更字段、枚举或导出 Schema | 完整 pytest 33/33；存储 Schema 等于 `ScanRun.model_json_schema()`；人工路径边界、diff 与敏感信息扫描 | 是 |
| 2026-09-01 | GPT-5.6 Sol / Codex Root | 根据项目负责人确认冻结 AI producer 字段，并固化进度、目录与 GitHub 发布规则 | P0 契约 v0.1.1、跨模型协作规则、项目进度台账 | 项目负责人明确批准三个字段；Terra/Luna继续实现与独立验证 | 契约差异审查、全量测试、Schema一致性、目录/敏感信息和 GitHub 待提交清单检查 | 是 |
| 2026-09-01 | GPT-5.6 Terra | 实现已冻结的 v0.1.1 AI `ProducerRef` 条件字段，并重导公开 Schema | `provider`、`model_id`、`prompt_schema_digest` 的模型约束、合成 sample 版本迁移和 10 项字段边界测试 | 仅实现已批准字段；sample 保持 `ai_enabled=false`，不伪造产品 AI 运行事实 | 全量 pytest 43/43；Pydantic/JSON Schema/sample 三重验证；Schema 导出一致、diff 与敏感信息检查 | 是 |
| 2026-09-01 | GPT-5.6 Luna | 独立回归 A1.1 AI `ProducerRef` 三字段、sample v0.1.1 与公开材料边界 | 完整字段有效、逐字段缺失、非 AI 显式 null/携带字段、空白/凭据、无效 SHA-256、AI provenance 及版本事实测试 | 未修改 backend、契约、Schema 或 sample；将进度文档中的匿名化规则词条与实际身份信息区分 | 全量 pytest 46/46；Pydantic/Draft 2020-12、Schema 导出一致、fixture/进度文档凭据和本机路径扫描、`git diff --check` 均通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 逐页核验三份正式竞赛 PDF，并基于冻结 P0 v0.1.1 设计 S0 评分证据治理与 S2/A2 安全门禁 | 竞赛要求、评分追踪、提交清单、九章证据映射、P0 非目标、威胁模型和 `SEC-A2-*` 验收基线 | 明确区分硬约束/评分/建议/内部决策；删除无证据效果主张；默认限额均标为尚未实现；不进入 A2 代码、不改变公共 Schema/API | PDF 全文/表格/逐页渲染核验；跨文档 ID/状态、来源页码、Markdown、敏感信息、本机路径和 `git diff --check`；待 Terra 可实现性和 Luna 负面测试独立审计 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 从主线实现角度审查 S2 的 20 项 A2 安全验收，不编写 A2 业务代码 | 可实现性结论、模块边界、Linux 部署前置条件、错误/清理映射和未来测试接口 | 未修改公共 Schema/API 或 Sol 安全语义；将 Git DNS-to-connect/transfer 配额和扫描器隔离标为部署级阻塞或修订建议 | 文档交叉引用、SEC 覆盖、Markdown、敏感信息/绝对路径与 diff 检查；未运行 A2 安全实现测试 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 根据 Terra 可实现性审查精确回修 A2 设计，不进入代码或 fixture | 直接 Git 零重定向、TrustedEgress 字节/连接证据、Linux 完成 profile、实际供应链范围、durable registry 条件和 ZIP 未知属性策略 | 保持 P0 v0.1.1、20 个 SEC、5 个 POS、36 个 NEG 不变；报告证据明确 Terra 已审、Luna 待审；未把设计或审查写成实现效果 | 审查文档逐项映射、SEC/POS/NEG 计数、`git diff --check`、敏感信息/绝对路径检查；A2 实现和 Luna 独立审计仍待执行 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立审计 A2 POS/NEG 设计的可测性、边界、错误/状态、真实集成和证据条件 | `docs/security/a2-test-audit.md`：逐 ID 判定、阈值拆分、TrustedEgress/Linux/durable registry 阻塞项、fixture 授权匿名脱敏模板与 S2 冻结结论 | 未修改 A2 实现、fixture、公共 Schema/API、PROJECT_PROGRESS 或安全语义文档；明确本轮未运行 A2 实现测试 | 文档交叉核对 Terra 12 ACCEPT/6 ADJUST/2 BLOCK 与 Sol 回修；ID/状态/Markdown/diff/敏感信息/绝对路径检查已完成 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 在冻结 A2 安全基线内实现本地 ZIP 安全输入纵切；输入为 A2 验收、实现审查和既有 P0 代码，不读取或运行不可信目标仓库内容 | POSIX 能力失败关闭、服务器端 ZIP 限额、dirfd workspace、ZIP 路径/类型/配额预检、流式物化、稳定 inventory/root digest 和实现侧动态 ZIP 测试 | 未修改 P0 v0.1.1 Schema/API、sample 或 Sol 规范；普通未知 ZIP 属性只作为新普通文件字节，未恢复元数据；不实现 Git、TrustedEgress、Linux 隔离或 durable registry | 聚焦 A2 ZIP pytest 16/16、全量 pytest 62/62；人工复核 root digest 字节格式、成功/失败清理与稳定错误 code/reason；后续由 Luna 独立扩展 TOCTOU、ZIP64/header corpus 和 Linux/egress 证据 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立运行 A2-0/A2-1 本地 ZIP 安全测试；仅使用现有实现与安全验收作为输入，不修改 Terra backend 或既有 unit 测试 | `tests/security/test_a2_zip_security_independent.py` 与 `tests/security/README.md`：标准库动态 ZIP、ZIP64/data descriptor/header、路径/类型/配额/清理/inventory/no-follow 边界 | 保留按冻结 reason 断言的失败测试，不为通过而放宽期望；未修改 P0 模型、Schema/sample、backend、既有 tests 或 PROJECT_PROGRESS | 独立测试 35 项：21 passed、14 failed；全量 97 项：83 passed、14 failed。失败已按稳定 reason 漂移和 local/central header mismatch 分类升级；`git diff --check`、敏感信息和绝对路径检查待本轮收尾完成；未运行目标项目代码 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 按 Sol 裁决复测 A2-0/A2-1；仅将深度/UTF-8 路径长度两项测试 code 期望改为 `archive_limit_exceeded`，复核 Terra 的 13 项 reason 与 local/central header 修复 | 保留同一标准库动态 ZIP、ZIP64/data descriptor/header 与文件系统安全测试；未新增不透明 fixture | 未修改 backend、Terra unit、P0/Schema/sample 或 PROJECT_PROGRESS；未弱化安全边界 | 独立测试 `35 passed`；Terra ZIP unit `18 passed`；当前全量 `99 passed`；排除 Terra 本轮新增 2 项单测后，历史可比的 97 项为 `97 passed`、`2 deselected`；`git diff --check`、敏感信息和绝对路径检查随后完成 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立补测 SEC-A2-009 home shorthand 路径边界；仅增加首段 `~`/`~user` 拒绝与后续普通文件名波浪号允许的安全回归 | `tests/security/test_a2_zip_security_independent.py` 新增动态 ZIP/ inventory 用例；无第三方或不透明二进制 fixture | 未修改 backend、Terra unit、P0/Schema/sample 或 PROJECT_PROGRESS；不扩大“仅首段”窄规则 | 独立测试 `36 passed`；Terra ZIP unit `19 passed`；当前全量 `101 passed`；P0 `46 passed`；`git diff --check`、尾随空白、敏感信息与本机绝对路径检查通过 | 是 |

记录原则：

- 不记录密钥、账号、未公开代码或个人信息；
- 说明模型参与环节，不把 AI 生成内容当然视为团队知识产权；
- 代码必须记录测试、人工审查或修改；
- 报告和图表必须核对原始数据；
- 最终整理为技术报告中的“AI 工具使用说明”。

## 追加记录

| 日期 | 工具/模型 | 任务 | 产出 | 人工复核与边界 | 验证方式 | 已脱敏 |
|---|---|---|---|---|---|---|
| 2026-09-02 | GPT-5.6 Terra | 修复 Luna A2 ZIP 独立测试揭示的稳定错误契约和 local/central header 完整性问题 | 统一重复、特殊类型和七类配额的内部 `code/reason`；加入 local header、ZIP64 尺寸与数据描述符交叉校验；补充实现侧回归和模块说明 | 未修改 Luna 独立测试、P0 Schema/sample 或 PROJECT_PROGRESS；路径深度和长度以冻结验收的 `archive_limit_exceeded` 为准，故 Luna 当前两个 `invalid_archive` 期望保留为契约冲突 | Terra ZIP 18/18；Luna 独立 33/35；全量 97 passed、2 failed；P0 Schema/sample/export 等值、diff、敏感信息与本机绝对路径检查 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 终审 A2-0/A2-1 本地 ZIP 实现、冻结错误语义和证据边界 | 最小澄清 `SEC-A2-009` 的 invalid/limit 分流；新增模块映射、支持矩阵、测试演进、复现命令和待 Root 分配实现证据位的审计材料 | 未修改 backend、测试、P0 Schema/sample/API 或 PROJECT_PROGRESS；将 `~`、完整 ZIP corpus、inventory 并发改写和清理隔离列为开放差异，未把 99 项绿灯外推为 A2 总门禁完成 | 全量与聚焦 pytest、P0 零差异、稳定 ID、diff、敏感信息及本机绝对路径检查；`EVD-S2-DESIGN-001` 仅作设计追溯 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 Sol 终审指出的 `SEC-A2-009` home shorthand 缺口 | 首段 `~` 或 `~user` 失败关闭为 `invalid_archive/archive_path_unsafe`；`ordinary/file~.txt` 保持允许；补充实现侧回归和说明 | 未修改 Luna 独立测试、P0 Schema/sample、公开 API 或 PROJECT_PROGRESS；Luna 尚未覆盖此新增边界，须后续独立补测 | Terra ZIP 19/19、Luna 独立 35/35、全量 100/100、P0 46/46、Schema/sample/export 等值、diff 与敏感信息/绝对路径检查 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 刷新 A2 ZIP 终审材料以接入 home shorthand 修复与独立回归闭环 | 将首段 `~`/`~user` 从开放差异移为已关闭，保留 21/14→35/0 历史链并追加当前 36/101 口径 | 未修改 backend、tests、P0 Schema/sample/API 或 PROJECT_PROGRESS；完整 ZIP corpus、inventory 并发、清理隔离和系统级门禁继续开放 | 文档计数/开放项一致性、diff、尾随空白、敏感信息与本机绝对路径检查；候选实现 evidence 仍待 Root 分配 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现本地 ZIP→inventory 命令行演示入口 | `python -m app.cli LOCAL_ZIP`：稳定 JSON 成功输出、稳定拒绝 stderr/退出码、临时 workspace 生命周期及实现侧回归 | 不联网、不执行目标代码、不安装其依赖、不开放限额抬升参数；不新增 HTTP API/P0 字段，Luna 仍须独立验证 CLI | CLI 5/5、Terra ZIP 19/19、Luna 36/36、全量 106/106、P0 46/46、Schema/sample/export 等值、diff 与敏感信息/绝对路径检查 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证本地 ZIP CLI 的稳定输出、受控拒绝、错误脱敏、退出码与无残留 | 新增独立 CLI 安全回归，覆盖重复运行、有效/穿越/缺失/目录/非 ZIP/错误参数和模块子进程 | 未修改 backend、Terra 单测、P0 Schema/sample、安全验收或 PROJECT_PROGRESS；只使用动态标准库 ZIP，不联网、不执行目标代码 | Luna CLI 5/5、Terra CLI+ZIP 24/24、全量 111/111、P0 46/46、Schema export 等值、diff 与敏感信息/绝对路径检查 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 终审本地 ZIP CLI 的产品边界、失败脱敏、默认限额、清理、稳定输出、退出语义与评委解释口径 | 在既有 ZIP 实现终审中增加 CLI 审计矩阵、`verified-local-demo` 候选证据条件和未跟踪 DOCX 上传边界 | 未修改 backend、tests、P0 Schema/sample/API 或 PROJECT_PROGRESS；未读取本地 DOCX；不把 CLI 外推为 Web、许可证扫描或 A2 总门禁；发现 Luna 收工声明与 `tests/security/README.md` 实际差异，证据冻结待 Root/Luna 更正 | 实跑 CLI 0/1/2、Luna 5/5、Terra CLI+ZIP 24/24、全量 111/111、P0 46/46 与 Schema/sample/export 等值；diff、P0 零差异和发布边界检查 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 关闭 `BLOCK-EVIDENCE-FREEZE-DOC-TRACEABILITY`：补齐独立 CLI 测试说明并更正 1334 记录的实际状态 | `tests/security/README.md` 现已明确复现命令、5 项覆盖范围、动态标准库 ZIP/无网络/无目标代码约束及当前 `5 passed`/全量 `111 passed` 真实口径；历史记录保留，不改写 | 仅修改安全测试 README 与本追加日志；未修改 backend、其他测试、P0、安全规范或 PROJECT_PROGRESS；无新 evidence_id、提交或推送 | 独立 CLI `5 passed in 0.13s`；全量 `111 passed in 0.38s`；`git diff --check`、敏感信息与本机绝对路径检查待收尾复核 | 是 |
| 2026-09-02 | Codex Root Coordinator | 统一验收并准备发布本地 ZIP CLI 演示 | 复跑真实 ZIP 得到单行稳定 inventory JSON；补充根 README 当前可运行状态、进度与本地竞赛源文档上传忽略规则；批准候选 `EVD-A2-ZIP-CLI-001` 待提交绑定 | 不读取或上传根目录 DOCX；不把 CLI 外推为 Web、依赖/许可证分析或 A2 总门禁 | 全量 111/111、P0 46/46、Schema 等值、compileall、有效/缺失输入 0/2、diff 与上传范围检查 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 冻结 A2-2 安全只读扫描会话的仓库内部契约 | `docs/spec/a2-readonly-scan-session.md`：向后兼容入口、inventory 白名单只读 capability、descriptor-relative/no-follow 打开、身份 seal 与内容 SHA-256 重验证、服务端配额、过期/并发语义、稳定脱敏错误及 `8 POS + 24 NEG` | 未修改 backend、tests、P0 Schema/sample/API 或进度；明确仅允许可信非执行性 parser，不把进程内 API 冒充不可信 Python sandbox、TrustedEgress 或 Linux 隔离证据 | 与现有 A2 源码、单测、Luna 独立测试及 `SEC-A2-009/012/013/016/017/019/020` 交叉审查；Markdown/ID/错误字面量、P0 零差异、`git diff --check` 和物理 EOF 日志检查 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现 A2-2 生命周期绑定的 ZIP 只读消费会话 | 增加服务端读取限额、会话 capability、回调前后 inventory/SHA 重验证、过期/线程/重入失败关闭和实现侧回归 | 不新增 P0/API，不把可信 callback 视为安全沙箱；Luna 仍须真实文件系统独立验证 TOCTOU、清理优先级与完整 NEG 矩阵 | 会话+既有 ZIP/CLI 27/27、全量 114/114、P0 46/46、Schema 等值、compileall 与 diff 检查 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 裁决 A2-2 扫描单文件默认值与旧 ZIP 配置的真实兼容冲突 | 向只读会话规格追加 v0.1.1 AMENDMENT：`scan_single_file_read_max_bytes: int | None = None`，省略/`None` 解析为 `min(2 MiB, single_file_max_bytes)`，显式整数仍严格验证且不静默 clamp | 保留 v0.1.0 历史文本与旧 Luna 测试；未修改 backend、tests、P0 或 PROJECT_PROGRESS；只覆盖单文件扫描配置的声明默认/派生规则 | 核对三种构造语义：标准默认有效 2 MiB、旧 ZIP 1 MiB 配置派生 1 MiB、显式 2 MiB 搭配 ZIP 1 MiB 仍 `ValueError`；规格 ID/差异/P0 范围检查 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证 A2-2 安全只读扫描会话，保留上游实现缺陷证据 | 新增 `tests/security/test_a2_readonly_scan_session_independent.py`，覆盖路径/外部 sentinel、symlink/FIFO/目录与 TOCTOU、配额、生命周期/线程/重入/并发、异常脱敏、清理优先级、ZIP 映射、BaseException 和公开能力面 | 未修改 backend、tests/unit、Sol 规格、P0 或 PROJECT_PROGRESS；使用真实临时文件系统与受控故障注入，不联网、不执行目标代码、不放宽断言 | 独立用例 44 项：41 passed、3 failed；全量 173 项：170 passed、3 failed；P0 46/46、Schema 等值 true、compileall 通过；失败为 open 错误映射、close 错误静默吞掉、公开暴露 `failure`/`expire`，已升级 Terra/Sol | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：复跑并关闭 A2-2 三项独立验证阻塞 | Terra/Root 修复后原样复跑 `tests/security/test_a2_readonly_scan_session_independent.py`，确认 open/close 稳定错误语义与 session 公开能力面符合规格；安全 README 已将 41/3 更新为 44/44 | 未修改 backend、tests/unit、Sol 规格、P0 或 PROJECT_PROGRESS；未提交、未推送；保留原 BLOCKED 历史，不改写；Linux/TrustedEgress、cleanup quarantine/orphan、完整 ZIP corpus、Git/Web/API 和 A2 总门禁仍未证明 | 独立 `44 passed in 0.07s`；全量 `173 passed in 0.51s`；P0 `46 passed in 0.11s`；`schema_export_equal=true`；compileall、`git diff --check`、尾随空白、敏感信息与本机绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | A2-2 安全只读扫描会话最终架构、安全与竞赛证据终审 | 确认共享 ZIP materialization、descriptor-relative/no-follow、精确白名单、身份/哈希 seal、派生与显式限额、累计预留、生命周期及异常优先级主路径；补充两项真实故障探针 | 不修改 backend、tests、P0、规格或 PROJECT_PROGRESS；发现根 `tree` descriptor 瞬态 open 错误错误映射为 `workspace_integrity_failed`，以及受控目标 fd close 失败后 descriptor 仍存活，两项 P1 阻止 evidence 批准；Linux/TrustedEgress、worker/quarantine、Git/Web/API 和 A2 总门禁仍未证明 | Luna 44/44、Terra 会话+ZIP 37/37、Terra CLI 5/5（合计 42）、全量 173/173、P0 46/46、Schema 等值 true、compileall/diff/发布边界通过；额外探针稳定复现两项 P1 | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：复跑并关闭 Sol 1524 指出的两项 descriptor P1 | 新增两项独立回归：消费前验证后的瞬态根 `tree` descriptor open 错误稳定脱敏为 `scan_file_read_failed`；目标 file fd 首次 close 失败经受控 recovery 后外层失败且 fd 为 `EBADF`；安全 README 同步更新最终计数 | 未修改 backend、tests/unit、Sol 规格、P0 或 PROJECT_PROGRESS；保留 1501 BLOCKED、1524 BLOCKED 历史；Linux/TrustedEgress、cleanup quarantine/orphan、完整 ZIP corpus、Git/Web/API 和 A2 总门禁仍未证明 | 独立 `46 passed in 0.09s`；全量 `175 passed in 0.52s`；P0 `46 passed in 0.11s`；`schema_export_equal=true`；compileall、`git diff --check`、尾随空白、敏感信息与本机绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | AMENDMENT：终审关闭 15:24 两项 descriptor P1 | 复核根 `tree` open 稳定映射、目录/文件 fd 所有权列表、seal 校验 deferred recovery、回收失败 service poison 与两项 Luna 独立回归；原 BLOCKED 历史保留 | 未修改 backend、tests、规格、P0 或 PROJECT_PROGRESS；候选证据仅批准待 Root 绑定不可变提交与运行 profile，不外推既有非目标 | Luna 46/46、Terra 42/42、全量 175/175、P0 46/46、Schema 等值 true、compileall/diff/P0 零差异与发布扫描通过；未发现新 P1 | 是 |
| 2026-09-02 | Codex Root Coordinator | 统筹并验收 A2-2 安全只读扫描会话 | 在 Terra 首版基础上补齐 sealed descriptor reader、错误仲裁、兼容限额、close 不确定所有权恢复与 service poison；组织 Luna 两轮独立缺陷发现/复跑和 Sol 两轮终审；更新运行说明与进度 | 不执行目标代码、不联网、不修改 P0；只批准本地可信进程内只读会话纵切，不外推 Linux/TrustedEgress/Git/Web/B1 或 A2 总门禁 | Root 全量 175/175、Luna 46/46、定向 unit 42/42、P0 46/46、内存 ZIP 实际消费成功、compileall/diff/P0 零差异与上传范围检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 冻结 B1-1 Python manifest parser 架构、契约、安全与验收门禁 | 新增设计规格：仅消费 A2 inventory/read_bytes；冻结 requirements/pyproject 语义、不可变 DTO、Evidence/P0 映射、配额/部分成功/稳定错误、`packaging==26.3` 依赖门禁及 `12 POS + 24 NEG` | 未修改 backend、tests、P0 Schema/sample、既有冻结规格或 PROJECT_PROGRESS；未安装依赖、未执行目标代码、未联网；设计保持 `IMPLEMENTATION_UNVERIFIED` | 逐项交叉核对正式材料/P0/A2-2；唯一 ID 12/24、Markdown fence、diff、受保护路径和敏感信息/本机绝对路径静态检查 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现 B1-1 Python manifest parser 纵切 | 新增只消费 A2 inventory/read_bytes 的确定性 parser，覆盖 requirements/pyproject、不可变 DTO、字段/行级 evidence draft、URL/选项安全拒绝、配额、去重与冲突；精确锁定 `packaging==26.3` | 未执行目标代码、未安装目标依赖、未联网、未打开目标真实路径；未修改 P0/Schema/sample、Sol 规格、Luna 独立测试或项目进度；仍待 Luna 独立验证及 Root 集成门禁 | 实现侧 36/36 B1 matrix、内存 ZIP→A2 session→parser 一致性、A2/P0 定向回归 83/83；完整结果和最终状态待收工检查记录 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证 B1-1 Python manifest parser；使用冻结规格、Terra 实现和 A2-2 只读会话作为输入 | 新增 12 POS + 24 NEG 的独立安全回归与两份小型文字 fixture；覆盖真实内存 ZIP→A2 session→parser、限额前置拒绝、能力/marker/副作用/过期/packaging 门禁和证据/冲突语义 | 不复用 Terra helper/期望；未修改 backend、Terra unit、Sol 规格、P0/Schema/sample 或 PROJECT_PROGRESS；不联网、不安装依赖、不执行目标代码；失败按实现缺陷交 Terra、契约语义交 Sol | 独立 `36 passed`；Terra B1 `36 passed`；全量 `247 passed`；P0 `46 passed`；compileall、`git diff --check`、授权文件变化和材料扫描收尾复核 | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：按 Root 集成审查补充 B1 冻结语义的精确独立回归 | 在既有独立测试中新增 DTO `None` 类型/值、URL dot-segment/重复 key、EOF 反斜杠、顶层 dependency-groups、意外异常稳定失败、重复 inventory path 与 canonical raw declaration 检查；不新增冻结 ID | 首轮原样执行保留实现失败证据；未修改 backend、Terra unit、Sol 规格、P0/Schema/sample 或 PROJECT_PROGRESS；不放宽断言、不提交推送；待 Terra 处理实现缺口 | 加固选择集 `15 failed`、`36 deselected`；失败逐项对应冻结行为缺口，未继续运行全量或 Terra；失败输出已保留并在协作日志逐项映射 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 Luna B1 AMENDMENT 的 15 项实现缺口 | 将 evidence 行号、bare specifier 与 diagnostic path 对齐可空 DTO 契约；拒绝 URL dot-segment/重复 subdirectory、EOF 悬空 continuation 与顶层 dependency-groups；补齐异常脱敏、重复 inventory 失败关闭和 canonical raw declaration | 未修改 Luna 独立测试、Sol 规格、third_party、P0/Schema/sample 或进度；不联网、不执行目标代码、不安装目标依赖；仍待 Luna/Root/Sol 后续验收与证据绑定 | Luna AMENDMENT `15 passed`、独立全文件 `51 passed`、Terra `38 passed`、全量 `264 passed`、P0 `46 passed`；compileall、diff、敏感信息和绝对路径检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立复核 Terra 对 B1 AMENDMENT 15 项实现缺口的修订 | 原样复跑独立全文件；逐项核对 DTO 可空值、URL dot-segment/重复 key、EOF 反斜杠、dependency-groups、异常脱敏、duplicate inventory 和 canonical raw；新增/修改仅安全说明、AI 记录与共享日志 | 独立断言未被修改或放宽；未修改 backend、Terra unit、Sol 规格、P0/Schema/sample 或 PROJECT_PROGRESS；不提交推送 | AMENDMENT `15 passed`；独立全文件 `51 passed`；Terra B1 `38 passed`；全量 `264 passed`；P0 `46 passed`；compileall、`git diff --check`、敏感信息/绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：响应 Root 二次探针，补充 raw canonical、URL 空 segment 与非 list dynamic 独立回归 | 仅在既有独立测试中新增 3 组加固断言，不新增冻结 ID；覆盖规范化名称/marker、空 segment/percent decode 与 `manifest_field_invalid` | 首轮原样执行保留实现失败；未修改 backend、Terra unit、规格、P0/Schema/sample 或 PROJECT_PROGRESS；不放宽断言、不提交推送 | 加固选择集 `5 failed, 15 passed, 36 deselected`；失败均对应实现缺口，未运行后续全量，等待 Terra | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 Luna 二次 B1 amendment 的 raw、URL 与 dynamic 缺口 | raw 改用 normalized name/extras/specifier 与 packaging marker；subdirectory 拒绝空段；`project.dynamic` 先验证为 `list[str]` | 未修改 Luna 测试、规格、P0/Schema/sample、进度或第三方台账；发现旧 POS-003 raw marker 引号期望与新 canonical 要求冲突，未以特判绕过 | 二次 amendment `20 passed`、Terra unit `38 passed`、P0 `46 passed`；独立全文件仍 1 failed，因旧 POS-003 与新 canonical marker 文本冲突；未执行全量 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 关闭 B1 FINAL-001..005 五项 P1 实现缺口 | 物理行边界、canonical raw/excerpt 1000 上限、extras collision、None 空 bytes 排序和 IPv6 bracket canonical URL 修订 | 未修改 Luna 测试、Sol 规格、P0/Schema/sample、进度或 third_party；仍待 Luna 五项逐字面独立复核 | Terra unit `40 passed`、全量声明 `271 passed`；未批准 evidence | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立复核 B1 FINAL-001..005 五项 P1 | 新增 5 组逐字面断言，映射既有 POS-002/004、NEG-003/011/020；不复用 Terra helper，不改变冻结 ID 数量 | 初次选择中的状态附加断言经核对属于测试侧误加，修正为冻结的多约束 warning；核心排序/边界断言未放宽；未修改 backend、Terra unit、规格、P0/进度 | P1 选择 `5 passed`；独立全文件 `61 passed`；Terra `40 passed`；全量 `276 passed`；P0 `46 passed`；compileall、diff、敏感信息/绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：响应 Root 补充 FINAL-001 leading/trailing U+2028 探针 | 仅为现有 FINAL-001 增加 leading/trailing 参数，保持中间分隔断言、测试名和冻结 ID 不变 | 原样运行后保留 2 项实现失败；未修改 backend、规格、P0/进度，不放宽断言、不提交推送 | 选择 `1 passed, 2 failed, 60 deselected`；leading/trailing U+2028 被错误 strip 并接受为依赖，已追加 BLOCKED | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：按 Sol 单点裁决修正 POS-003 canonical marker 期望并复跑 | 仅将单引号期望改为 `a==1 ; python_version > "3"`，保留测试名、行号断言、冻结 ID 和其余独立断言 | 未修改 backend、Terra unit、B1 规格、P0/Schema/sample 或 PROJECT_PROGRESS；不提交推送 | 独立全文件 `56 passed`；Terra B1 `38 passed`；全量 `269 passed`；P0 `46 passed`；compileall、diff、敏感信息/绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | B1-1 最终架构、安全与竞赛证据终审 | 原样复跑 56/38/269/P0 46 与 Schema/compileall/diff 门禁；以只读内存探针发现 Unicode 行分隔、raw 1000 上限、extras canonical 去重、冻结排序键和 IPv6 URL canonical 五项 P1，规格状态更新为 `FINAL_AUDIT_BLOCKED` | 未修改 backend、Terra/Luna tests、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；不代修、不批准候选 evidence，不外推 P0 mapper/许可证/Web/Linux/TrustedEgress/A2 总门禁 | Python 3.12.13、packaging 26.3；Luna 56、Terra 38、全量269、P0 46 均通过；五项探针输出与 P0 命令路径 P2 已写入审计 AMENDMENT | 是 |
| 2026-09-02 | Codex Root Coordinator | 统筹 B1-1 Python manifest parser 设计、实现、独立缺陷闭环与发布验收 | 组织 Sol 冻结12 POS/24 NEG、Terra实现、Luna独立测试和多轮未覆盖边界探针；核验 `packaging==26.3` 官方来源/许可证；复跑真实 ZIP→只读会话→parser 与全套回归 | 不执行或安装目标代码、不联网；不修改 P0 v0.1.1；只批准本地可信 consumer 的 Python requirements/pyproject DTO 层，不外推 mapper、JS/lockfile、许可证、Web、Linux/TrustedEgress | Terra40、Luna独立63、全量278、P046、Schema等值、真实ZIP两次结果相等且只读两个候选、diff/敏感路径通过；待不可变提交绑定与GitHub推送 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 Sol B1 FINAL_AUDIT_BLOCKED 五项 P1 | 改为仅 CRLF/LF/CR 物理行分割；1000 codepoint raw 边界失败关闭；extras canonical 去重 UTF-8 排序；identity 字段字节排序；IPv6 URL 保留方括号 | 未修改 Sol 规格、Luna 测试、P0/Schema/sample、进度或第三方台账；不联网、不执行目标代码、未提交推送；仍待 Luna 独立探针与 Root/Sol 复审 | Luna 现有 `56 passed`、Terra `40 passed`、全量 `271 passed`、P0 `46 passed`；compileall、diff、敏感/绝对路径检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | B1-1 FINAL-001..005 关闭后的最终复审与候选 evidence 裁决 | 只读核对五项修复和 Luna 逐字面探针；规格追加 CLOSED AMENDMENT，批准 `EVD-B1-PYTHON-MANIFEST-001` 为 `APPROVED-PENDING-ROOT-BINDING` | 未修改 backend/tests/P0/进度/third_party；批准仅限本地可信 A2 consumer parser，绑定前不进入发布或报告主张，不外推 JS/lock/P0 mapper/许可证/Web/Linux/TrustedEgress/A2 总门禁 | P1 `5 passed`、Luna `61 passed`、Terra `40 passed`、全量 `276 passed`、P0 `46 passed`、Schema 等值；自定义五探针、compileall、diff、敏感/绝对路径及受保护路径检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 FINAL-001 leading/trailing U+2028 词法空白边界 | requirements 裁剪、注释判定、continuation 与 token 分割仅接受 ASCII space/tab；`_parse_requirement` 行号参数允许 None，移除未使用 TOML 行号 helper | 未修改 Luna/规格/P0/进度/台账；不联网、不执行目标代码，未提交推送 | FINAL-001 `3 passed`、Luna `63 passed`、Terra `40 passed`、全量 `278 passed`、P0 `46 passed`；compileall、diff、敏感/绝对路径检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：独立复核 Terra 对 FINAL-001 leading/trailing U+2028 的修复 | 原样复跑现有 FINAL-001 三参数，确认中间、leading、trailing U+2028 均不拆物理行、不被裁剪为有效空白；未新增冻结 ID | 未修改 backend、Terra unit、规格、P0/Schema/sample、PROJECT_PROGRESS 或第三方台账；不提交推送 | FINAL-001 `3 passed`；独立全文件 `63 passed`；Terra `40 passed`；全量 `278 passed`；P0 `46 passed`；compileall、diff、敏感信息/绝对路径扫描通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 冻结 B1-2 Python DTO 到 P0 与 CLI 兼容设计门禁 | 新增 mapper/CLI v0.1.0 规格，定义确定性 UUID、Component/Evidence 映射、locator 编码、partial diagnostics、时间注入、旧 CLI 字节兼容、新 JSON/退出/清理及 12 POS + 18 NEG | 未修改 backend/tests/P0/Schema/sample/PROJECT_PROGRESS；不推断 purl/许可证，不外推 JS/Web/Git/Linux/TrustedEgress 或 A2 总门禁；待 Terra/Luna/Root 实现验证 | UUID namespace 与 locator 示例复算；POS/NEG 唯一计数 12/18；Markdown fence、diff、whitespace、受保护路径与敏感信息检查 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现 B1-2 Python manifest DTO→P0 mapper 与本地 ZIP Python 依赖 CLI | 新增纯内存 mapper、确定性 UUIDv5/locator/Component/Evidence 映射；新增精确 `--python-dependencies LOCAL_ZIP` 模式、固定 clock、A2-2 consumer sentinel/cleanup 和 43 项实现侧回归（12 POS、18 NEG ID 可检索） | 未修改 P0 v0.1.1 模型/Schema/sample、Sol 规格、Luna 独立测试或 PROJECT_PROGRESS；不联网、不执行 ZIP 代码、不安装目标依赖；尚待 Luna 逐 ID 独立验证及 Root/Sol evidence 绑定 | B1-2 unit 43/43；B1 parser/A2 CLI/P0 重点集 187/187；全量 321/321；compileall、diff、敏感信息/绝对路径检查通过；真实磁盘 ZIP complete/partial、固定时间和无残留由实现测试覆盖 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证 B1-2 Python P0 mapper 与本地 ZIP Python 依赖 CLI | 新增 `tests/security/test_b1_python_p0_mapper_cli_independent.py`，仅使用标准库动态 ZIP 和 A2-2 `ingest_with_consumer`；逐 ID 验证冻结 12 POS + 18 NEG、known-answer、P0 字段、locator/diagnostic/partial、旧 CLI、错误/清理/副作用边界 | 未修改 backend、Terra unit、B1-2 规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；未复用 Terra helper 生成 expected UUID/locator/JSON/error；不联网、不安装目标依赖、不执行目标代码 | Luna `30 passed`；Terra B1-2 `43 passed`；B1 parser `40 passed`；A2 CLI `10 passed`；P0 `46 passed`；全量 `351 passed`；compileall、diff、敏感信息/绝对路径检查通过；待 Root/Sol 终审和不可变 evidence 绑定 | 是 |
| 2026-09-02 | GPT-5.6 Sol | B1-2 最终架构、安全与竞赛证据只读审计 | 逐项核对 mapper/CLI 与 43 项 Terra、30 项 Luna；补充 tampered DTO 探针，发现 optional locator round-trip 捕获缺陷及 frozen DTO canonical 验证不完整两项 P1 | 未修改 backend/tests/P0/Schema/sample/PROJECT_PROGRESS/third_party；不批准 `EVD-B1-PYTHON-P0-CLI-001`，不外推本地 Python P0 CLI 纵切 | 现有 Luna30、Terra43、B1-1 103、A2 10、P0 46、全量351均通过，Schema等值、compileall/diff/受保护路径/敏感扫描通过；额外探针稳定复现非 canonical locator、重复 evidence、query URL、declared name 与任意敏感 diagnostic 被接受 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 修复 Sol `FINAL-B1P0-001/002` 两项 mapper P1 | optional group 完整捕获并 canonical percent round-trip；在 P0 构造前拒绝重复/非排序 EvidenceDraft、非 canonical name/raw/specifier/marker/hash/scope/source/reference、非固定或敏感 diagnostic 及不匹配的 field/path/line 组合 | 未修改 B1 parser、P0/Schema/sample、Sol 规格、Luna tests 或进度；不联网、不执行 ZIP 代码、不安装目标依赖；仍待 Luna 增补独立探针和 Root/Sol 复审 | 先复现两项 probe 为 ACCEPTED，修复后均为 `scanner_failed:python_p0_mapper_failed`；Terra 45/45、Luna 30/30、B1-1+P0 149/149、全量 353/353、Schema 等值、compileall/diff 通过；敏感 pattern 仅命中拒绝性测试字面量与检测正则 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立复核 B1-2 终审 P1 FINAL-B1P0-001/002 | 在独立测试中新增两个 P1 补充组：手工 DTO 拒绝非 canonical 完整 optional group，以及拒绝重复 EvidenceDraft、declared_name/raw/query URL/任意敏感 diagnostic 篡改；同步复核合法 optional group、diagnostic、direct/VCS 不回归 | 未修改 backend、Terra unit、规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；expected failure 固定为 `scanner_failed:python_p0_mapper_failed`，不复用 mapper helper 生成期望值 | 新增选择 `2 passed`；Luna `32 passed`；Terra `45 passed`；B1-1 `103 passed`；P0 `46 passed`；全量 `355 passed`；Schema 等值、compileall、diff、敏感信息/路径检查通过；仍待 Root/Sol evidence 绑定 | 是 |
| 2026-09-02 | GPT-5.6 Sol | 关闭复审 B1-2 `FINAL-B1P0-001/002` 并裁决候选 evidence | 只读复现原七项 tampered DTO 探针，逐分支核对 frozen validator 与合法 parser/CLI 路径，规格追加 CLOSED AMENDMENT | 未修改 backend/tests/P0/Schema/sample/PROJECT_PROGRESS/third_party；批准仅为 `APPROVED-PENDING-ROOT-BINDING`，不得提前发布或外推 | 七项探针全部脱敏拒绝；Luna32、Terra45、P046、全量355通过；Schema等值、compileall/diff/受保护路径/敏感与空白检查通过 | 是 |
| 2026-09-02 | GPT-5.6 Sol / Codex Root Coordinator | B1-3/B1-4 JavaScript manifest、P0 与 CLI 最小契约冻结 | Sol 完成 B1/A2/P0/CLI 风险面梳理；Root 因长时设计中断接管并冻结单一规格，限定 package.json 与 package-lock v2/v3、严格 JSON、npm 字段/selector、lock enrichment、Evidence/UUID/P0、JS CLI 和 10 POS + 16 NEG | 不实现、不测试；明确排除 npm lock v1、shrinkwrap、Yarn、pnpm、workspace 跨包、依赖求解/安装、许可证和合规结论；证据保持未验证 | ID 计数 10/16、Markdown/whitespace 检查；后续必须由 Terra 实现、Luna max 独立验证、Root 绑定后才能形成运行事实 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现 B1-3/B1-4 JavaScript manifest、P0 mapper 与本地 ZIP CLI 纵切 | 新增严格 JSON package parser、lock v2/v3 根直接依赖 enrichment、冻结 DTO/P0 UUID/locator/purl/HTTPS 映射和精确 `--javascript-dependencies` CLI；新增动态 ZIP 35 项实现回归 | 未修改 P0/Schema/sample、冻结规格、Luna tests、PROJECT_PROGRESS 或 third_party；不调用 Node/npm、网络或目标代码，不安装依赖；仍待 Luna 独立逐 ID 验证与 Root/Sol evidence 绑定 | Terra JS 35/35；Python/A2/P0 聚焦 271/271；全量 390/390；Schema 等值、compileall、diff 检查通过；敏感命中仅为检测正则与标识符 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证 B1-3/B1-4 JavaScript manifest、P0 mapper 与 CLI；输入为冻结规格、Terra 2150 COMPLETE 和 A2-2 只读会话 | 新增 `tests/security/test_b1_javascript_manifest_p0_cli_independent.py`；以动态 JSON/ZIP、手写 UUID/locator/JSON/error expected 逐 ID 覆盖 10 POS + 16 NEG，并验证真实 ZIP、P0 reload、旧 CLI、错误/清理/副作用边界 | 5 项独立 P1 失败原样保留：`~` range 被拒绝、dot-segment URL 被接受、inventory size/forged manifest size/空 locator token 未拒绝；未修改 backend、Terra unit、规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party，不提交推送 | 独立 `27 collected: 22 passed, 5 failed`；Terra JS `35 passed`；`git diff --check` 通过；按独立门禁未运行/宣称全量或发布 evidence，待 Terra/Sol/Root 处理与复测 | 否（待 P1 关闭与 Root 绑定） |
| 2026-09-02 | GPT-5.6 Terra | 修复 Luna B1-3/B1-4 五项 P1 与 Root JS mapper 加固探针 | 接受合法 `~` range；拒绝 dot-segment HTTPS、inventory size/read 不一致和 JSON NaN/Infinity；mapper 现在严格验证 manifest size/kind/UTF-8 顺序、npm 名称/selector、source/lock 同目录关系、canonical URL、空 pointer token、Evidence/diagnostic/status 组合 | 未修改 Luna 独立测试、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；不联网、不执行或安装 Node/npm 目标代码，不提交推送 | 先原样复现 Luna `5 failed` 与 Root 四类 probe；修复后 Luna 定向 `5 passed`、Root probes 通过、JS 实现+独立 `64 passed`、Python/A2/P0 `236 passed`、全量 `419 passed`、Schema 等值、compileall/diff 通过 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 按 Terra 2146 AMENDMENT 独立复核 B1-3/B1-4 修复，并追加不增加冻结 ID 数量的加固断言 | 原样复跑既有 27 项；在同一 security 测试文件追加 NaN/Infinity、手工 DTO name/selector、非 UTF-8 manifest 排序、kind/filename、source/lock 同目录和 canonical URL 检查 | 未修改 backend、规格、P0/Schema、Terra unit、PROJECT_PROGRESS 或 third_party；未复用 Terra helper；不提交推送 | 原有 27 项 `27 passed`；加固选择 `5 passed, 27 deselected`；Luna 全文件 `32 passed`；Terra JS `37 passed`；JS 合计 `69 passed`；Python/A2/P0 聚焦 `355 passed`；全量 `424 passed`；Schema 等值、compileall、diff、敏感模式检查通过 | 否（待 Root/Sol 终审与不可变绑定） |
| 2026-09-02 | GPT-5.6 Sol | 依据 P0 v0.1.1 与技术执行书人员边界冻结 A3-0 持久 ScanRun 注册表 | SQLite canonical JSON、create/get/replace/list、请求指纹幂等、revision/CAS、状态单向性、重启恢复、损坏/未知版本、路径权限、脱敏错误、关闭语义及 8 POS + 16 NEG 规格 | 保持六个 P0 API 与 ScanRun 不变；明确不实现 backend/tests/HTTP/worker/Pipeline，不接管扫描分析组员 B2-B7 或前端组员任务；当前仅设计未验证 | P0 状态机/字段逐项核对、技术执行书表格只读复核、ID 唯一计数、Markdown fence、diff/受保护路径/敏感信息静态检查；待 Terra 实现与 Luna 独立验证 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 实现 A3-0 单机 SQLite 持久 ScanRun 注册表 | 新增标准库 SQLite registry：canonical JSON BLOB、create/get/replace/list/close、fingerprint 幂等、revision CAS/no-op、状态与时间单向性、WAL/FULL、私有路径校验、损坏/schema/busy/close 脱敏错误；新增动态 SQLite 回归 | 未修改 P0/Schema/sample、冻结规格、Luna 测试、PROJECT_PROGRESS 或 third_party；不实现 HTTP、worker、A4 Pipeline、扫描分析组员 B2-B7 或前端组员任务，不提交推送 | A3 单测 `30 passed`；A3+P0 `76 passed`；全量 `454 passed`；Schema 等值、compileall、diff 与敏感检查待收工复核 | 是 |
| 2026-09-02 | GPT-5.6 Terra | 关闭 Luna A3 schema 声明与约束 hardening P1 | `_verify_schema` 现精确校验 metadata/scan_runs 的 PRAGMA 列序、类型、notnull/default/pk，scan_id 主键、idempotency_key 唯一索引和 revision `CHECK >= 1` 的规范化建表 SQL；任何额外、缺失或错误声明失败关闭 | 未修改 Luna 独立测试、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；不新增 HTTP/worker/Pipeline，不提交推送 | 修复前 Luna hardening `1 failed`；修复后 Luna 全文件 `31 passed`、Terra A3 `31 passed`、A3+P0 `77 passed`、全量 `486 passed`；Schema 等值、compileall、diff/敏感检查待收工复核 | 是 |
| 2026-09-02 | GPT-5.6 Luna | 独立验证 A3-0 单机 SQLite 持久 ScanRun 注册表；输入为冻结 A3 规格、P0 v0.1.1 与 Terra 实现交接 | 新增 `tests/security/test_a3_scan_registry_independent.py`，按冻结 `8 POS + 16 NEG` 覆盖 canonical BLOB、跨 connection/instance 幂等、revision/CAS、状态时间单向性、分页、重启、手工 SQL/字节损坏、POSIX 路径、并发、close/activity 和错误脱敏；expected 由独立手工构造，不复用 Terra helper | 未修改 backend、Terra unit、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；保留 schema 类型/约束篡改的 P1 实现失败，不提交推送；仅更新安全说明、AI 记录和共享工作日志 | 独立全文件 `31 collected: 30 passed, 1 failed`；失败为同名错误 schema 未被 `registry_schema_unsupported` fail closed；按门禁未运行 Terra/P0/全量，未批准 A3 evidence | 否（待 Terra 修复、Luna 复测与 Root/Sol 绑定） |
| 2026-09-02 | GPT-5.6 Luna | AMENDMENT：独立复核 Terra 2325 的 A3 schema fail-closed 修复并补最小结构探针 | 在现有独立测试中新增 12 个 metadata/scan_runs 类型、notnull、PK、revision CHECK、幂等 UNIQUE、额外 index/列篡改 case，以及 1 个合法库 close/reopen case；不改变冻结 ID 或放宽既有断言 | 未修改 backend、Terra unit、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；仅追加安全说明、AI 记录和共享日志，不提交推送 | hardening `14 passed`；独立全文件 `44 passed`；Terra A3 `31 passed`；A3 合计 `75 passed`；P0 `46 passed`；全量 `499 passed`；`schema_export_equal=True`；compileall、diff、尾随空白、敏感模式和权限检查通过 | 否（P1 已关闭，待 Root/Sol 不可变提交绑定与有界 evidence 裁决） |
| 2026-09-02 | GPT-5.6 Sol | A3-0 候选 evidence 最终契约审计 | 逐段核对冻结规格、registry、Terra/Luna 测试与 8 POS + 16 NEG；额外以临时 SQLite 注入 trigger/额外表，发现 `FINAL-A3-001` schema 对象 allowlist 缺口 | 未修改 backend/tests/P0/Schema/sample/PROJECT_PROGRESS/third_party；不追加 CLOSED AMENDMENT，不批准 evidence；首次 Luna schema 声明/约束 P1 的关闭事实保留 | A3+P0 定向 `121 passed`；但合法两表后注入 `AFTER INSERT` trigger 或额外表均被重开接受，trigger 使 create 返回 revision 1 而持久行变为 999；候选阻塞待 Terra 修复、Luna 独立复测、Sol/Root 重审 | 否（开放 P1） |
| 2026-09-02 | GPT-5.6 Terra | 关闭 Sol `FINAL-A3-001` sqlite_master 对象 allowlist P1 | `_verify_schema` 现验证所有非 SQLite 内部对象严格等于 `registry_metadata` 与 `scan_runs` 两表；零 view/trigger、零额外用户表，现有 PK/UNIQUE 自动索引验证保持；额外对象统一 `registry_schema_unsupported` | 未修改 Sol/Luna 测试、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；不新增 HTTP、worker、Pipeline、扫描分析组员 B2-B7 或前端组员任务，不提交推送 | 修复前 trigger 复现 create revision 1 / get revision 999；修复后 trigger 重开稳定拒绝；Luna `44 passed`、Terra A3 `32 passed`、A3+P0 `78 passed`、全量 `500 passed`；Schema 等值、compileall、diff/敏感检查待收工复核 | 是 |
| 2026-09-02 | GPT-5.6 Luna | FINAL-A3-001：独立复验 Terra sqlite_master 对象 allowlist 修复；输入为 Sol 原始 P1 探针与 Terra 2345 COMPLETE | 在现有独立测试中加入额外用户 table、view、`AFTER INSERT` revision trigger 的原始重开探针，并验证移除对象后合法库保持 revision 1；未改变冻结 `8 POS + 16 NEG` ID 或既有失败断言 | 未修改 backend、Terra unit、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；只更新独立测试、安全 README、AI 记录与共享日志，不提交推送 | 原始 probe `1 passed`；Luna 全文件 `45 passed`；Terra A3 `32 passed`；A3 合计 `77 passed`；P0 `46 passed`；全量 `501 passed`；`schema_export_equal=True`；compileall、diff、敏感检查通过；仍待 Sol/Root evidence 绑定 | 否（待 Sol/Root 最终裁决） |
| 2026-09-02 | GPT-5.6 Sol | FINAL-A3-001 关闭复审与 A3-0 候选 evidence 裁决 | 核对 sqlite_master 严格对象 allowlist、Terra 回归和 Luna 原始 table/view/trigger 探针；向冻结规格 EOF 追加 CLOSED AMENDMENT | 未修改 backend/tests/P0/Schema/sample/PROJECT_PROGRESS/third_party；批准仅为 `APPROVED-PENDING-ROOT-BINDING`，不可提前发布或外推 | Sol A3 `77 passed`；Root 报告全量 `501 passed`、P0 `46 passed`、Schema 等值、compileall/diff 通过；无开放 P0/P1 | 是 |
| 2026-09-02 | Root | 将 Sol 批准的 A3-0 候选证据绑定到不可变实现提交与运行 profile | `EVD-A3-DURABLE-SCAN-REGISTRY-001` 绑定 `d2b26b0897978d156a461abae97e163a6cb3564d`；记录 macOS/POSIX、CPython 3.12.13、SQLite 3.53.1 与完整门禁摘要 | 仅批准单机 SQLite ScanRun 注册表；不外推 FastAPI/HTTP、worker/A4、Linux/TrustedEgress、多机、组员任务或完整作品 | A3 `77 passed`、P0 `46 passed`、全量 `501 passed`、Schema 等值、compileall/diff/隐私与上传范围检查通过；无开放 P0/P1 | 是 |
| 2026-09-03 | Root | 按技术执行书实现项目负责人 A3-1 FastAPI 最小 Git API 纵切 | 应用工厂、六条冻结业务路由、只读 DTO、Git queued 创建与持久幂等、结果投影/过滤、统一错误、运行/测试/第三方说明 | 未修改 P0 Schema/sample、A2/B1扫描实现或前端；不实现ZIP、联网克隆、worker、Pipeline、扫描器/规则/AI或报告生成，不把测试注入终态冒充产品结果 | A3-1 `17 passed`、A3/P0联合 `140 passed`、全量 `518 passed`；真实 Uvicorn POST 202、GET 200、停止后 SQLite 重开仍为 queued；Schema/范围/敏感检查待提交前收口 | 是 |
| 2026-09-03 | GPT-5.6 Luna | 独立复核 A3-1 FastAPI Git API；输入为冻结 P0/A3-1 路由、A2 URL 安全边界和 Root 候选实现，不采信 Sol 结论而以独立 expected/探针复测 | 新增独立 TestClient/SQLite/Uvicorn 测试，覆盖六路由、queued 持久幂等、ResourceView/风险/证据/报告投影、过滤、未就绪状态、错误信封/request_id、未知路由/方法、URL 控制字符与 UTF-8 byte 上限；修正测试夹具自身 `ProducerType.HUMAN` 枚举误用 | 未修改 backend、Terra/Root unit、冻结规格、P0/Schema/sample、PROJECT_PROGRESS 或 third_party；失败原样保留，不代修、不放宽断言、不提交推送；真实 Uvicorn 仅在受控回环环境单独运行 | 独立全文件 `25 collected: 17 passed, 8 failed`，其中 7 个稳定实现 P1 断言（`FINAL-A3API-001..003`）与 1 个沙箱回环绑定限制；独立 Uvicorn `1 passed, 24 deselected`；未运行 A3/P0/全量或 Schema 收口，候选 `EVD-A3-FASTAPI-GIT-API-001` 保持 `BLOCKED-P1` | 否（待 Terra/Root 修复、Luna 原始复测、Sol/Root 重审与绑定） |
| 2026-09-03 | Root / GPT-5.6 Sol / GPT-5.6 Luna | 关闭 A3-1 独立复核的 `FINAL-A3API-001..003` | Root 最小统一 Starlette 404/405 错误信封、拒绝原始/解码控制字符并执行 2048 UTF-8 bytes 上限；Luna 原始独立探针不变复测；Sol 重审证据边界 | 未新增路由/错误码/业务功能，未进入真实 Git/ZIP、worker/A4、B1-B7、前端、AI 或报告；候选先标 `APPROVED-PENDING-ROOT-BINDING` | Luna `24 passed, 1 deselected`，Root 受控回环全量 `549 passed`；Schema 等值、compileall、diff、受保护路径与权限检查通过；1 条第三方弃用 warning 保留 | 是（待 Root 不可变提交绑定） |
| 2026-09-03 | GPT-5.6 Terra | 实现冻结 A4-0 显式单进程 Pipeline Worker | 新增 `PipelinePlan`/`PipelineStep`、稳定 `PipelineError`、显式 `PipelineStageFailure` 与 CAS 驱动 Worker；固定七阶段、进度、completed/partial/failed/cancelled、clock 与脱敏错误边界，并新增实现侧回归 | 不新增默认 production stub，不启动后台线程/轮询，不自动消费 A3 API queued；未修改 P0/A3、冻结规格、A2/B1-B7、前端、progress 或 third_party | A4 `21 passed`，A4+A3+P0 `99 passed`；全量在沙箱中 `569 passed, 1 loopback bind denied`，受控回环复测 `1 passed, 24 deselected`；Schema 等值、compileall、diff/敏感检查通过。仅可表述为本机单进程注入 Adapter 的 durable 编排，不代表真实扫描或完整产品 | 是 |
| 2026-09-03 | GPT-5.6 Luna | AMENDMENT：独立复测 Root 对 A3-1 `FINAL-A3API-001..003` 的修复 | 原样复跑独立 TestClient/SQLite 探针；复核 Starlette 404/405 统一信封、原始/解码控制字符拒绝及 UTF-8 byte 上限；未删除或放宽上一轮失败断言 | 本轮未修改 backend、Root/Terra unit、规格、PROJECT_PROGRESS、third_party 或 P0/Schema/sample；仅追加安全说明、AI 记录和共享日志，不提交推送；回环授权未再次申请，采用 Root 受控验证结果 | 独立排除回环项 `24 passed, 1 deselected`；A3-1 实现+独立 `47 passed, 1 deselected`；A3-0 `77 passed`；P0 `46 passed`；全量排除回环项 `548 passed, 1 deselected`；Schema `1 passed`；compileall/diff/尾随空白/敏感/world-writable 通过；Root 受控回环全量 `549 passed`；三项 P1 已关闭，候选 evidence 待 Sol/Root 重审绑定 | 否（待不可变提交、运行 profile 与有界范围重审/绑定） |
| 2026-09-03 | GPT-5.6 Luna | 独立复核冻结 A4-0 Pipeline Worker；输入为 A4-0 规格、Terra `1245 COMPLETE`、实现与 unit，不修改 Terra 实现 | 新增 `tests/security/test_a4_pipeline_worker_independent.py`，独立构造 queued `ScanRun`/SQLite/完整七阶段 plan，逐 ID 覆盖 POS-A4-001..005、NEG-A4-001..010 及持久化、并发、脱敏、CAS、clock/registry 故障边界；同步补充安全说明与本记录 | 未修改 backend、Terra unit、冻结规格、P0/Schema/sample、A3/A2/B1-B7、前端、PROJECT_PROGRESS 或 third_party；不复用 Terra helper，不提交推送；证据限于本机单进程可信 stage adapter | 独立 `25 passed`；A4 实现+独立 `46 passed`；A3/P0 `170 passed, 1 deselected`；全量 `594 passed, 1 deselected`；Schema `1 passed`；compileall、diff、Luna 范围尾随空白、敏感模式和 world-writable 检查通过；回环 deselect 原因已记录；无新 P0/P1/P2，A4 候选 evidence 待 Sol/Root 绑定范围 | 否（待不可变提交、运行 profile 与有界范围裁决） |
| 2026-09-03 | GPT-5.6 Sol / Terra / Root | 冻结并实现项目负责人 A4-1 本地 ZIP 依赖 Pipeline 接线 | Sol 冻结一次性计划、同一 A2 会话双语言调用、随流摘要、稳定合并与 rules partial；Terra 两轮形成并修正骨架但以 PARTIAL 收工；Root 在同一边界恢复中断文件、关闭合法 Path、真实 consumer/close、P0 合并与实现测试缺口 | 只调用既有 B1 公共接口，不修改扫描分析组员 B1-B7 内部或前端；不新增 HTTP ZIP、后台队列、许可证规则、AI、报告、网络或第三方依赖；当前结果必须停在 partial，不伪造 completed | 实现侧真实动态 ZIP、A2/B1/A3/A4/SQLite 测试 `29 passed`；保护回归 `566 passed`；待 Luna 独立验证、全量、Schema/compileall/静态门禁与不可变提交绑定 | 否（待独立验证与 Root 发布） |
| 2026-09-03 | GPT-5.6 Luna | 独立验证冻结 A4-1 本地 ZIP 依赖流水线；输入为冻结规格、Root 实现侧 29 项结果、既有 A2/B1/A3/A4-0 公共接口 | 新增 `tests/security/test_a4_local_zip_pipeline_independent.py`：动态 ZIP、手工 queued P0 run、真实 A2 只读 consumer、B1 双路 parser/mapper、A4 worker、SQLite 重开；覆盖 5 POS/10 NEG 与参数化变体、错误脱敏、cleanup、digest、rules partial 和 AI/report 不执行 | 仅修正独立夹具自身的 worker 时钟/预创建 0700 workspace 根；未修改 backend、Root/Terra unit、冻结规格、P0/Schema/sample、A2/B1/A3/A4-0、PROJECT_PROGRESS、third_party 或前端；不复用实现侧 helper/expected，不提交推送 | Luna 独立 `20 passed`；A4-1 unit+独立 `49 passed`；全量排除沙箱回环项 `643 passed, 1 deselected`；Schema 等值、compileall、diff 检查通过；未发现新 P0/P1/P2；A4-1 候选 evidence 待 Root/Sol 绑定与裁决 | 否（待 Root 不可变提交、运行 profile 与有界发布） |
| 2026-09-03 | GPT-5.6 Sol / Root | 解释 A4-1 `partial/rules/70` 并实现项目负责人 A3-2 ZIP HTTP 后台纵切 | 冻结同一路径 JSON/multipart 分流、安全请求总量与上传流限额、私有暂存、SHA-256/幂等、queued 创建、进程内 BackgroundTask→A4-1、清理、OpenAPI 与 20 项实现测试；核验并锁定 python-multipart | 不改变 partial/P0 Schema/A2/B1-B7/A3 registry/A4 worker/plan 或前端；不代做许可证规则；明确 BackgroundTask 非持久队列且不具备 lease/retry/recovery/Linux/TrustedEgress | 实现侧 `20 passed`；A3/A4-1 保护集 `96 passed, 1 deselected`；待完整回归、Luna 独立验证、真实回环、静态门禁和证据绑定 | 否（实现候选，待独立验证） |
| 2026-09-03 | GPT-5.6 Luna | 独立验证冻结 A3-2 ZIP multipart HTTP 创建与进程内 BackgroundTask→A4-1；输入为 Root 1725 COMPLETE、A3-2 候选实现/unit、冻结规格及既有 A2/B1/A4-1 公共接口 | 新增 `tests/security/test_a3_zip_background_scan_independent.py`：独立动态 ZIP、multipart、SQLite、真实 A2+B1+A4、资源/证据查询、幂等/限额/字段/路径/控制字符/清理/脱敏/OpenAPI/Git JSON/默认权限，以及可单独复跑的真实 Uvicorn ZIP 202→terminal→resources→重开探针 | 未修改 backend、实现侧 unit、冻结规格、P0/Schema/sample、A2/B1-B7、A3 registry、A4 worker/plan、PROJECT_PROGRESS、third_party、前端或原始附件；不复用实现侧 helper/expected，不提交推送 | 独立非回环 `21 passed`；受控真实 Uvicorn 单项 `1 passed`；A3-2 unit+独立非回环 `41 passed`；全量非回环 `684 passed, 2 deselected`；Schema、compileall、diff、权限/范围检查通过；无新 P0/P1/P2；候选 `EVD-A3-ZIP-BACKGROUND-SCAN-001` 待 Root/Sol 绑定 | 否（待不可变提交、运行 profile 与有界发布） |
| 2026-09-02 | GPT-5.6 Sol（扫描组员分支） | 实现跨来源合并与 ScanCode/Syft 受限 JSON 适配候选 | 外部工具调用禁用 shell，限制超时与输出；ScanCode 只生成待标准化的许可证证据候选，Syft 在有相对定位时生成 Component；冲突合并保留证据 | 未改变 P0 Schema；不执行目标代码、不捆绑工具、不伪造许可证或风险；由项目负责人从提交 `e244588` 纳入集成候选 | 原分支仅静态验证；集成分支必须在当前 Python 3.12 环境重新执行测试，真实工具运行仍单独验收 | 是（作为组员候选纳入 `integration/p0`，待集成门禁） |
| 2026-09-04 | GPT-5.6 Sol / Terra / Root | 冻结并实现项目负责人 A5-0 AI Provider 与确定性降级边界 | 新增 transport-neutral Provider、canonical finding/evidence/license 输入、严格 JSON/引用/敏感内容校验、pending Remediation、UUIDv5 稳定身份及 generated/skipped/disabled/degraded 结果 | Terra 两轮形成骨架并诚实以 PARTIAL 收工；Root 在同一冻结范围补齐结构、原子性和实现测试；未接真实模型/网络/A4，不修改 P0/API/组员 B4-B6 或前端，不把 AI 建议作为许可证或法律事实 | A5+P0 `75 passed`；全 unit `368 passed`；待 Luna 独立验证、完整安全回归、Schema/静态门禁和不可变提交绑定 | 是（候选，待独立验证） |
| 2026-09-04 | GPT-5.6 Luna | 独立验证冻结 A5-0 AI Provider 与确定性降级边界 | 新增 `tests/security/test_a5_ai_provider_independent.py`，独立构造 P0/Provider/expected；覆盖合法 local/remote、pending/stable evidence、disabled/skipped/no-call、严格响应/引用/敏感/路径、异常/timeout、64 KiB、批次原子性、事实保持、metadata snapshot 与 repeated degradation | 未修改 backend、tests/unit、P0/Schema/sample/API/pipeline、B1-B7、前端、PROJECT_PROGRESS 或 third_party；修正的仅是独立测试自身夹具/断言；无提交推送；保留无效 P0 聚合失败，不放宽断言 | 独立 `16 collected: 15 passed, 1 failed`；失败为 provider 已调用 1 次后从 `_degraded` 逸出的未包装 `ValidationError`，对应 P1；按门禁未扩大回归，候选 `EVD-A5-AI-PROVIDER-001` 保持 `BLOCKED-P1` | 否（待 Terra 修复、Luna 原始复测与 Root/Sol 绑定） |
| 2026-09-04 | GPT-5.6 Luna | AMENDMENT：独立复测 Root 对 A5-0 P1 的入口重校验修复 | 原样复跑独立 16 项；确认事后变异 P0 在 Provider 执行前返回 `ai_invalid_argument`，并执行 A5 unit+独立、全 security 非回环、P0 Schema、compileall/diff/范围/敏感门禁 | 未修改 backend、tests/unit、独立断言、P0/Schema/sample/API/pipeline、B1-B7、前端、PROJECT_PROGRESS 或 third_party；不提交推送；保留原 BLOCKED 历史，不把本地注入 Provider 外推为真实模型 | 独立 `16 passed`；A5 unit+独立 `46 passed`；全 unit/security `734 passed, 2 deselected`；P0 `46 passed`；compileall/diff/受保护路径/权限/敏感检查通过；原 P1 关闭，候选 evidence 待 Root/Sol 不可变绑定 | 否（待不可变提交、运行 profile 与有界发布） |
| 2026-09-04 | GPT-5.6 Sol / Root | A5-0 最终边界审计与候选 evidence 裁决 | 复核 Provider 快照、严格输入输出、证据允许集、原子 pending Remediation、P0 入口重校验、稳定降级和实现/独立测试；更新可运行说明 | 仅批准无网络的可注入 Provider 核心；不把 mock Provider 当作真实 Qwen3/Ollama，不宣称 A4 已接线、许可证规则正确或产品闭环完成；没有新增第三方运行依赖 | Root 复跑完整非回环 `734 passed, 2 deselected`、`schema_export_equal=True`、compileall/diff/上传范围与敏感检查；`EVD-A5-AI-PROVIDER-001` 批准待不可变提交绑定 | 是（待 Root 提交与推送） |
| 2026-09-04 | GPT-5.6 Luna | 独立验证冻结 A5-1a Qwen3/Ollama 本地 Transport；自建有界 TCP fixture，不调用 Ollama | 新增 `tests/security/test_a5_ollama_transport_independent.py`，独立硬编码版本 `0.33.3`、模型名和完整 manifest/blob digest；覆盖三步 HTTP、请求字段、禁代理、pending/degraded、真实 timeout、HTTP 错误、loopback origin 和停止/临时物边界 | 未修改 backend、unit、Sol 规格、P0/Schema/sample、A2-A4、B1-B7、frontend 或 `PROJECT_PROGRESS.md`；第三方仅登记未安装/未下载/未运行候选；保留 sandbox 原始 bind 失败，不提交推送 | 独立 `20 collected: 12 passed, 8 failed`；8 项均为 `ThreadingHTTPServer` 绑定 `127.0.0.1:0` 的 `PermissionError`，未运行 A5 unit/全 security/Schema/compileall；`EVD-A5-OLLAMA-TRANSPORT-001` 为 `BLOCKED-ENV`，待 Root 受控回环复跑 | 否（待受控复跑与 Root/Sol 绑定） |
| 2026-09-04 | GPT-5.6 Luna / Codex Root | AMENDMENT/COMPLETE：更正 A5-1a 独立测试数量并记录受控回环复跑 | 保留原始 sandbox 失败事实，将当前独立测试口径更正为 `17 collected`、`8 passed`、`9` 项在真实 TCP fixture bind 处失败；Root 在受控回环环境原样复跑当前测试文件并完成 `17 passed in 4.70s` | 未修改 backend、unit、Sol 规格、P0/Schema/sample、A2-A4、B1-B7、frontend 或 `PROJECT_PROGRESS.md`；未调用真实 Ollama/Qwen3；原始 sandbox 失败记录不被覆盖 | Root 受控环境确认真实 TCP 三步顺序、请求体、禁代理、A5 pending/degraded、timeout、HTTP 失败脱敏与 shutdown/临时文件断言；证据仍只限有界协议 fixture 与本地 adapter 行为，不证明真实模型质量、许可证规则、A4 接线或完整竞赛作品 | 否（待 Root/Sol 绑定不可变提交、运行 profile 与有界证据） |
| 2026-09-04 | GPT-5.6 Sol / Terra / Luna / Root | 冻结、实现并终审 A5-1a Qwen3/Ollama 本地 Transport | Sol 锁定官方版本、模型/manifest 与安全协议；Terra 形成 adapter 骨架，Root 在同一范围完成标准库实现和 60 项 unit；Luna 独立构造有界 TCP fixture，Root 受控原样复跑 | 只允许字面量回环 HTTP、禁环境代理、固定身份和总 deadline；未安装/下载/调用真实 Ollama/Qwen3，未接 A4、未修改组员 B1-B7 或前端，不把协议 fixture 外推为模型能力 | A5 组合 `123 passed`；完整非回环 `794 passed, 2 deselected`；`schema_export_equal=True`、compileall、diff、受保护路径与权限门禁通过；候选 `EVD-A5-OLLAMA-TRANSPORT-001` 待不可变实现提交绑定 | 是（候选，待 Root 提交与推送） |
| 2026-09-04 | GPT-5.6 Luna | A5-1b 独立真实运行复验 | 无代理独立连接本机 Ollama `127.0.0.1:11434/api/version` 时，sandbox 原样返回 `PermissionError: [Errno 1] Operation not permitted`；已立即向 Root 报告并停止，不调用 runtime probe、Provider 或模型 | 未修改实现/unit/P0/Schema/sample/A2-A4/B1-B7/frontend；只追加 AI 记录与共享日志；不记录 prompt、完整 response、绝对临时路径或秘密 | `EVD-A5-OLLAMA-REAL-RUN-001` 为 `BLOCKED-ENV`；version/tags、磁盘 manifest/blob、`ollama ps`、三轮 generated/pending/稳定身份、成功率/延迟/资源均未取得；不证明真实模型失败、A4 接线、B5 规则或作品完整性 | 否（待 Root 受控环境原样复验） |
| 2026-09-04 | GPT-5.6 Sol / Terra / Luna / Codex Root | A5-1b 官方 Ollama/Qwen3 本机真实运行与独立复验 | Sol/Root 核验官方 `0.33.3` DMG 的 SHA-256、Developer ID、Gatekeeper、公证、架构与版本，拉取并重算锁定 Qwen3 manifest/blob；Terra 提供最小聚合 runtime probe 候选，Root 修正多 finding/事实保持/稳定身份边界并补 5 项 unit；Luna 先原样保留 sandbox 回环阻塞，再只写一次性独立脚本，由 Root 在受控环境执行 | 只验证项目负责人 A5 运行时；未接 A4、未实现组员 B5 许可证规则或前端；仓库不保存 DMG、权重、prompt、完整 response、密钥或本机路径。安装包/模型只存在于用户机器，AI 输出仍为 `pending` 而非许可证或法律事实 | 项目探针真实 `3/3`：冷 4344.062 ms、热 2736.214/2723.574 ms；Luna 独立脚本 SHA-256 `675c64de5620c6fd4fcc6714eb3cca30a08835d247f093934b0b53933c59462f`，受控执行 `3/3`：冷约 3877 ms、热均值约 2768 ms；manifest/API/disk/blob、generated、pending、producer、finding 引用、确定性事实和稳定 ID 通过；A5 `128 passed`、完整集合 `818 passed`；实现证据绑定 `ca0c3ed` | 是（已绑定，待治理提交与推送） |
| 2026-09-04 | Codex Root Coordinator | AMENDMENT：发布 A5-1b 不可变实现与治理证据 | 将实现 `ca0c3ed` 与治理 `26ebdc8` 推送到 `origin/feat/a5-ollama-transport`，并用远端只读查询核对分支指向 `26ebdc8c783adb2cd6e344f02164bd2abee422e1` | 未创建/合并 PR，未修改 `main`、`integration/p0` 或组员分支；未上传安装包、权重、模型原文、临时独立脚本、缓存或凭据 | GitHub 功能分支已包含 A5-1b 源码、unit、运行说明、资源/第三方台账、聚合实测与 AI/协作/进度证据 | 是（已绑定并推送） |
| 2026-09-04 | GPT-5.6 Sol / Codex Root | A8-1c A5 团队集成门禁与 PR #2 | 从 `integration/p0` 建立仓库外隔离合并候选，复核 A5-0/A5-1a/A5-1b 的完整 PR 差异、回归、Schema、编译、隐私和公开范围，并创建 base=`integration/p0`、head=`feat/a5-ollama-transport` 的 PR #2 | 仅修正 A5 规格中的两处行尾空格并补治理记录；未修改业务语义、P0、A2-A4、B1-B7 或前端；保留沙箱回环权限失败，受控环境原样复跑；未请求组员评审、未合并 PR | 隔离合并无冲突；沙箱 `807 passed, 11 failed, 1 warning`，受控环境 `818 passed, 1 warning`；P0 `46 passed`；Schema、compileall、diff、敏感/路径/大文件/上传范围通过；GitHub 显示 PR #2 Open、Ready to merge、无冲突 | 是（PR #2 待审核，未合并） |
| 2026-09-04 | GPT-5.6 Sol / Codex Root | 在 VS Code 复现当前后端、A5 与 mock 前端 | 识别系统 Python 不满足要求并使用既有 Python 3.12.14 环境；在 VS Code 终端启动 FastAPI、提交自建最小 ZIP、轮询 SQLite 终态，运行真实本地 Ollama/Qwen3 聚合探针并启动/打开 Vite 页面 | 不修改业务代码、P0/Schema/sample、A2-A5、B1-B7 或前端；临时脚本、ZIP、SQLite 均在仓库外；不记录 prompt/完整 response；前端明确为 mock，A5 未接 Pipeline，`partial/rules/70` 不外推为许可证扫描完成 | ZIP POST `202`；3 个组件与 3 条 verified evidence 落库；终态 `partial/rules/70`；Qwen3 `2/2`，pending/事实保持/生产者/稳定身份全通过；Vite `127.0.0.1:5173` 可见且页面标注 `MOCK MODE` | 是（治理证据已推送当前 PR 分支；运行临时物未上传） |
| 2026-09-04 | GPT-5.6 Terra / Codex Root | 在 B5 尚未交付时实现项目负责人 A6-0 确定性报告导出核心 | 新增终态 `ScanRun` 到稳定 JSON、竞赛七字段 CSV/资源清单和安全静态 HTML 的纯内存导出，附媒体类型、稳定文件名与 SHA-256；补专项测试、冻结范围和复现说明 | 不实现或模拟 B5 许可证规则，不修改 P0/API/SQLite/Pipeline/A2-A5、组员 B1-B7 或前端；`partial/rules/70` 明示规则缺失，不把空 finding 外推为合规通过；不调用 Qwen3 | A6 专项 `12 passed`；A6+P0 `58 passed`；受控完整集合 `830 passed`；Schema 等值、compileall、diff、范围、敏感与大文件门禁通过；不可变实现绑定 `fda4ce6`，远端已核对 | 是（已绑定并推送） |
| 2026-09-04 | GPT-5.6 Terra / Codex Root | 在 A6-0 上实现项目负责人 A6-1 报告安全持久化、`ReportLink` 与只读下载 | 新增内容寻址 `ReportArtifactStore`、原子 metadata 提交、严格读取复核；默认应用创建私有报告目录；既有报告 GET 默认返回 link，`download=true` 只读返回附件与 digest/ETag/nosniff/no-store/CSP | 不修改 P0/Schema/sample、SQLite scan registry、Pipeline/A2-A5、组员 B1-B7、规则或前端；GET 不生成报告、不写数据库；`partial/rules/70` 保留阶段性说明，不伪造 B5 事实 | A6-1 `16 passed`；A6+A3 `51 passed`；A6+A3+P0 `97 passed`；沙箱原样 `835 passed/11 loopback bind denied`，受控原样全量 `846 passed`；compileall、Schema/diff/敏感/范围门禁通过；`EVD-A6-REPORT-DELIVERY-001` 绑定不可变实现 `9ce9535`，远端已核对 | 是（已绑定并推送） |
| 2026-09-04 | GPT-5.6 Terra / Codex Root | 实现项目负责人 A6-2 Pipeline 终态报告发布 | 新增 `PipelineReportPublisher`，在 worker 首次终态 CAS 前发布四格式并绑定完整 `ReportLink`；默认 ZIP HTTP 主链自动产出可重启下载的诚实 `partial/rules/70` 报告；API 以 registry link 为可见性门禁并校验 store metadata | 不实现/模拟 B5，不修改 P0/Schema/sample、SQLite状态机、扫描组员 B1-B7、A5、前端或部署；报告正文投影 delivery links 避免摘要自引用；GET 保持只读，orphan 不可见 | A6-2 专项 `10 passed`；A6/A4/A3/P0 联合 `177 passed`；沙箱原样 `845 passed/11 loopback bind denied`，受控原样全量 `856 passed`；`EVD-A6-PIPELINE-PUBLISH-001` 绑定不可变实现 `eec66a6`，远端已核对 | 是（已绑定并推送） |
| 2026-09-04 | GPT-5.6 Terra / Codex Root | 实现项目负责人 A2-3a 公开 Git 安全摄取与 TrustedEgress 纵切 | 新增公开 HTTPS URL/地址门禁、固定 TLS DoH、逐连接 CONNECT 出口、固定 Git no-checkout 浅克隆、`ls-tree`/`cat-file` 安全物化、revision/inventory/provenance，以及 API→B1/A4→A6 阶段性报告接线 | 不实现/模拟 B5，不修改 P0/Schema/sample、SQLite schema/状态机、组员 B1-B7、rules、A5、前端或部署；不支持私有仓库/SSH/重定向/submodule，不执行目标代码；Linux 隔离与持久队列继续明确未完成 | 离线 A2 单测通过；团队仓库真实 A2 成功后因无受支持 manifest 诚实停在 `failed/scan/35`；公开 PyPA sampleproject 完整纵切通过；沙箱 `858 passed, 9 loopback bind denied, 2 skipped, 2 deselected`，受控完整 `871 passed, 1 warning`；候选 `EVD-A2-PUBLIC-GIT-EGRESS-001` 待不可变提交绑定 | 是（候选，待 Root 提交与推送） |
| 2026-09-04 | GPT-5.6 Terra / Codex Root | AMENDMENT：A2-3a 最终地址端点与开关加固复测 | resolver 端点必须 family/IP 匹配且目标端口精确为 443；Git 禁用 replace objects；管理员联网开关只接受 `0/1`，新增回归后原样复跑 | 不改变上一行功能范围或公共契约；旧计数作为加固前历史保留，不改写 | A2 实现侧 `14 passed, 1 skipped`；沙箱 `859 passed, 9 loopback bind denied, 2 skipped, 2 deselected`；受控完整 `872 passed, 1 warning` | 是（候选，待 Root 提交与推送） |
| 2026-09-05 | GPT-6 Astra / Codex Root | 审阅组员 B5 并完成项目负责人 A4-2 规则阶段接线 | 获取并逐文件审阅组员分支截至 `f8bedfd` 的 B5 引擎、15条规则、fixture与规格；原样引入公共实现，以薄适配器完成 `ScanRun` 输入校验、B5 调用、聚合/引用/版本复核和 A4 rules 阶段持久化 | 不改写 B5 引擎或规则语义，不实现/导入 B4、B6、B7，不修改 P0 Schema、扫描器、前端或部署；无上游许可证事实时保留 `partial/rules/70`，不猜测许可证或伪造完整扫描 | B5 文件 Git blob 与组员远端逐一相同；A4+B5 聚焦 `68 passed`；沙箱完整原样 `877 passed, 11 loopback bind denied, 2 skipped`，受控完整 `888 passed, 2 skipped`；Schema 等值、compileall/diff 门禁通过 | 是（A4-2 候选，待不可变提交与推送） |
| 2026-09-05 | GPT-6 Astra / Codex Root | AMENDMENT：发布 A4-2 不可变实现并绑定远端证据 | 将 18 个竞赛仓库文件以提交 `4752f2b` 推送 `origin/feat/a4-b5-rule-integration`，并用远端只读查询核对完整对象一致 | 未创建或合并 PR，未修改 `integration/p0`、`main`、组员分支；未上传 B4/B6/B7、缓存、数据库、模型内容、凭据或本机临时物 | `EVD-A4-B5-RULE-INTEGRATION-001` 绑定 `4752f2b11252870c1b33306583390321c8d24397`；发布前完整受控 `888 passed, 2 skipped` | 是（已绑定并推送） |
| 2026-09-05 | GPT-6 Astra / Codex Root | 实现并验证项目负责人 A5-1c Pipeline `AI_ASSIST` 接线 | 把既有 A5 Provider/Ollama 以默认关闭、管理员显式启用方式传入 shared plan、ZIP/Git runtime 和默认应用；独立构造 B5 pending/verified 聚合、Provider 成功/失败及 A6 报告，并以锁定本机 Qwen3 实跑一项 | 不修改组员 B5 引擎/规则、B4/B6/B7、扫描器、P0 Schema/sample、前端或部署；AI 只生成 pending 建议，确定性整改不重复；首次真实单项因 Ollama 服务未启动保留失败，启动本机服务后原样通过并立即停止服务 | 实现 `9 passed`；独立默认 `5 passed, 1 skipped`；显式真实模型 `1 passed`；沙箱完整 `891 passed, 11 loopback denied, 3 skipped`，受控完整 `902 passed, 3 skipped`；Schema、compileall/diff/保护路径门禁通过 | 是（A5-1c 候选，待不可变提交与推送） |
| 2026-09-05 | GPT-5.6 Luna | 独立复核项目负责人 A5-1c B5→AI_ASSIST→SQLite→A6 门禁 | 新增 `tests/security/test_a5_pipeline_integration_independent.py`，独立手工构造 P0/B5 pending/verified、Provider/expected 和私有 SQLite/A6 报告；覆盖默认关闭、pending AI 整改、verified 确定性整改不重复、Provider 失败/无效响应降级、ZIP/Git 配置传递、非法配置和显式真实 Ollama 门控 | 未修改 backend 实现、P0 Domain/Schema/sample、`backend/app/rules/`、`rules/`、`backend/app/scanners/`、前端、部署或既有 unit；未提交/推送；真实 Ollama 项目项仅默认 skip，不自动启动或下载 | 独立 `9 passed, 1 skipped`；实现侧 `9 passed`；受控 A4/A5/A6/API/P0 保护集 `187 passed, 1 warning`；沙箱同组 `177 passed, 10 loopback bind denied`，失败均为既有回环监听环境限制；`git diff --check` 通过 | 是（独立 A5-1c 证据；普通 ZIP/Git 仍缺 B2/B3/B4 许可证事实，不外推完整产品） |
| 2026-09-05 | GPT-6 Astra / Codex Root | AMENDMENT：接纳 Luna A5-1c 独立验收并刷新最终门禁 | 复核 Luna 仅新增的独立安全测试，统一实现/独立/真实 Qwen3/完整回归口径；更正先前“Luna 未开始”和旧 6 项独立测试记录 | 不改写历史日志，不修改 B5、B2/B3/B4/B6/B7、扫描器、P0 Schema/sample、前端或部署；真实模型只临时启动本机服务，验证后立即停止 | 实现+独立 `18 passed, 1 skipped`；真实 Ollama `1 passed, 9 deselected`；沙箱完整 `895 passed, 11 loopback denied, 3 skipped`，受控完整 `906 passed, 3 skipped`；P0 `46 passed`、compileall/diff 通过 | 是（A5-1c 候选，待不可变提交与推送） |
| 2026-09-05 | GPT-5.6 Luna | AMENDMENT：补齐无效 Provider 响应的 A6 四报告验收 | 将独立降级管线参数化为 `ai_provider_unavailable` 与 `ai_response_invalid` 两种路径，均检查 SQLite 终态、B5 finding/规则事实保留及 HTML/JSON/CSV/resource_inventory 四种报告 | 仍只修改独立安全测试与 append-only 日志；未自动启动/下载 Ollama，显式真实用例继续默认 skip | 独立与实现侧合跑 `19 passed, 1 skipped, 1 warning`；独立文件单跑 `10 passed, 1 skipped`；新增静态敏感信息/绝对路径扫描无命中；`git diff --check` 通过 | 是（更新后的独立门禁；受控回环回归证据沿用上一条） |
| 2026-09-05 | GPT-6 Astra / Codex Root | AMENDMENT：按 Luna 最终文件完成 A5-1c 发布前统一复核 | 在 Luna 停止继续写入后固定最新独立测试摘要，复跑真实 Qwen3、沙箱完整、受控完整、P0 与静态门禁 | 不改组员 B5 或其他 B 模块，不把回环权限失败改成跳过，不上传模型、缓存、数据库、prompt 或完整 response | A5 实现+独立 `19 passed, 1 skipped`；真实 Ollama `1 passed, 10 deselected`；沙箱完整 `896 passed, 11 loopback denied, 3 skipped`；受控完整 `907 passed, 3 skipped`；P0 `46 passed`、Schema/compileall/diff 通过 | 是（A5-1c 候选，待不可变提交与推送） |
| 2026-09-05 | GPT-6 Astra / Codex Root | AMENDMENT：发布 A5-1c 并绑定远端不可变证据 | 将 15 个竞赛交付文件以实现提交 `3237ab0` 推送 `feat/a5-pipeline-integration`，远端完整对象与本地一致 | 未创建或合并 PR，未修改 `integration/p0`、`main`、组员分支、前端或部署；未上传模型/缓存/数据库/prompt/完整 response | `EVD-A5-PIPELINE-INTEGRATION-001` 绑定 `3237ab0e8634ba5f0c62535100ef97785bd611a6`；发布前受控完整 `907 passed, 3 skipped` | 是（已绑定并推送） |

| 2026-09-05 | GPT-6 Astra / Root；GPT-5.6 Sol、Terra、Luna | A3/A4-3a-S ZIP持久派发规格门禁；输入为既有代码、契约、完整历史日志与进度 | Root写唯一规格；Sol架构与历史复核、Terra可实现性、Luna独立15项oracle；均只读交付由Root统一记录 | 不新增业务代码、测试、公共Schema、模型或第三方依赖；关闭running links可见性与配额预留风险；保留profile/幂等、legacy边界；只批准规格，未获得用户最终产品发布许可 | 三角色最终APPROVE，本轮P0 46 passed；文档ID/引用/保护路径/append-only/diff/敏感门禁；动态恢复测试未执行，代码实现I1/I2未开始 | 是（设计文档，待Root文档分支发布；不是运行效果证据） |

| 2026-09-05 | GPT-6 Astra / Root | AMENDMENT：ZIP持久派发规格发布证据 | 规格提交f9a59fa3eb722c2eb1eb0ec939bda5efe8587b78已推送docs/a3-a4-durable-zip-spec，远端完整对象已只读核对 | 仅四个规格/治理文件；不修改产品代码、不创建或合并PR；三角色只读意见经Root逐条取舍与修订，未代替Owner最终作品审查 | P0 46 passed、15个独立门禁ID、Schema等值、append-only前缀、四文件保护范围、diff与新增敏感内容检查通过；持久worker动态测试未执行 | 是（已发布设计规格，代码实现未开始） |


| 2026-09-05 | GPT-6 Astra / Root；GPT-5.6 Terra、Luna | A3/A4-3a-I1 ZIP持久输入与派发记录 | 复用冻结规格/现有A3/A4/A5/A6；Terra实现、Luna独立动态ZIP/手写multipart/OS线程及进程验证，Root审查与最终回归 | 原并发P1与fsync测试定位问题均保留失败/AMENDMENT并闭环；不改P0/六API/registry/组员代码，不引入依赖或调用模型；生产durable仍关闭 | unit16、独立29、受控全量952 passed,3 skipped；沙箱11回环权限失败原样保留；OpenAPI/Schema/编译/前端构建通过；不是I2恢复或完整Web证据 | 是（Root技术验收通过，待不可变提交与推送绑定） |
