# P1 Contract Review Materials

Status: DRAFT — READY FOR PROJECT LEAD REVIEW

NOT FROZEN

- [契约草案](../spec/p1-workspace-contract.md)：九项决策等待项目负责人逐项审批。
- [对象样例](object-examples.json)：DRAFT / NON-AUTHORITATIVE EXAMPLE；仅用于 Contract Review，不是 runtime fixture，不是 Formal Schema，不是真实扫描证据。
- [历史验证回执](contract-validation-receipt.json)：原始回执按字节保留，仅记录起草时的 JSON 解析、example 结构、reference 和 git diff 检查；不代表 P1 功能测试通过或 Contract 已批准。

回执中的 branch、limitations 和 files 哈希属于发布前隔离草案状态。原始 Markdown 被原样附在发布状态说明之后，因此发布版 Markdown 的整体哈希与原始草案不同；object-examples.json 的字节及哈希保持不变。source-docx.txt 是未发布的本地提取文本，不是缺失的运行依赖。

本次只发布 Review 文档，不生成 schemas/p1/*.schema.json，不修改运行代码，也不授权 History、Diff、Graph、ResourceProfile、Remediation、NOTICE 或 Report V2 实现。正式契约冻结仍需后续审批。
