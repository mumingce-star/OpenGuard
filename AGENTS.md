# OpenGuard 多模型协作规则

本文件对所有在本仓库工作的 Codex 对话和模型生效。除用户最新明确指令外，本文件是多模型协作的共同操作契约。

## 1. 强制开工流程

每次开始任务前，模型必须按顺序：

1. 阅读根目录 `README.md`；
2. 完整阅读 `docs/coordination/AGENT_WORKLOG.md`，不得只看最后一条；
3. 阅读 `docs/coordination/PROJECT_PROGRESS.md`，确认已完成、在途、未开始和 GitHub 发布状态；
4. 阅读自身模型对应的交接文档：
   - Sol：`docs/handoffs/SOL_HANDOFF.md`
   - Terra：`docs/handoffs/TERRA_HANDOFF.md`
   - Luna：`docs/handoffs/LUNA_HANDOFF.md`
5. 检查当前 Git 分支、工作区状态和最近提交；
6. 确认其他模型已经完成、正在进行和尚未开始的工作；
7. 在共享日志末尾追加一条 `START` 记录，声明本轮范围、预计修改文件、验收方法和 token 用量估算，再开始修改。

如果日志显示另一个模型正在修改同一文件、Schema 或接口，当前模型不得直接覆盖；应缩小范围、等待、或写明需要协调的阻塞项。

## 2. 强制收工流程

每次任务结束前，无论结果是完成、部分完成还是阻塞，模型都必须在 `docs/coordination/AGENT_WORKLOG.md` 末尾追加一条报告，状态只能是：

- `COMPLETE`：验收条件已全部满足；
- `PARTIAL`：完成部分工作，明确剩余内容；
- `BLOCKED`：无法继续，明确阻塞原因和解除条件。

结束报告至少包含：

- 作者模型和对话角色；
- 日期时间；
- 任务目标和实际结果；
- 修改或新增文件；
- 运行的命令与测试结果；
- 新增或改变的接口、Schema、规则和重要决策；
- 已知风险、失败项和未完成内容；
- 建议下一步及责任模型；
- 关联的分支、提交、PR、Issue 或 evidence_id（如有）。

模型向用户回复“已完成”之前，必须先写结束报告。只做只读调查时也要写报告，但应明确“未修改项目文件”。

每个模型收工后，Root Coordinator 还必须在当前用户对话中给出项目进度表，至少列出本轮完成项、累计完成项、未完成项、责任模型、验证结果、GitHub 发布状态和下一步。

收工汇报还必须包含 token 使用说明：若系统提供本轮精确 token 遥测，报告精确值；若系统未提供，则明确写明“本次运行精确 token 数不可获得”，并回报开工估算区间、是否在该范围内完整完成及范围调整情况。不得以账号用量百分比、上下文窗口、预算标签、字符数或字数冒充精确 token 消耗。

## 3. 日志写入规则

- 共享日志只允许追加，不得删除、重排或改写历史记录；
- 发现旧记录错误时，追加 `AMENDMENT` 记录进行更正；
- 写入前必须重新读取日志末尾，避免覆盖其他模型刚追加的内容；
- 每条记录必须使用唯一 ID：`YYYYMMDD-HHMM-模型-简短任务名`；
- 作者必须写具体模型，例如 `GPT-5.6 Sol`、`GPT-5.6 Terra`、`GPT-5.6 Luna`；
- 不得在日志中写入学校、指导教师、密钥、令牌、个人信息或无权公开内容。

## 4. 模型职责边界

- Sol：竞赛需求、架构、Schema、风险语义、评测设计和最终审计；
- Terra：后端、前端、扫描器、AI 接入、报告和部署的主线实现；
- Luna：测试、fixtures、OpenGuard-Bench、资源台账、复现和材料检查；
- 跨职责变更必须在日志中说明影响，并标记需要谁复核。

## 5. Git 协作约束

- `main` 只通过 Pull Request 合并；
- 开始任务时记录工作分支，结束时记录提交或 PR；
- 一个分支只解决一个主要问题；
- 不覆盖其他模型或用户的未提交修改；
- 合并冲突时以正式竞赛规则、已冻结接口契约和最新用户决策为准；
- 共享日志出现冲突时保留双方完整记录，按时间顺序重新追加。
- Sol、Terra、Luna 默认不得自行推送；模型完成并写收工记录后，由 Root Coordinator 统一验收、整理、提交和推送任务分支；
- 每个通过验收的任务点都要推送到 GitHub，并在日志和 `PROJECT_PROGRESS.md` 记录分支、提交和上传文件范围；
- 只提交竞赛作品实际需要的源代码、规则、测试、Schema、样例、文档和部署文件；禁止上传临时目录、缓存、虚拟环境、本机绝对路径、密钥、个人信息、未脱敏竞赛材料或无权再分发内容；
- 推送前必须运行相关测试、`git diff --check`、敏感信息检查和待提交文件清单复核；
- `main` 仍只通过 Pull Request 合并；推送功能分支不等于已合并或可发布。

## 6. 完成判定

代码生成不等于任务完成。只有在实现、测试、文档、第三方资源登记、AI 辅助记录、共享工作日志、项目进度表和 GitHub 任务分支都更新后，任务点才可对用户标记为完成。

## 7. 竞赛目录整理规则

- 根目录只保留稳定的竞赛工程入口；新建顶层目录前必须说明用途，优先使用现有 `backend/`、`frontend/`、`rules/`、`benchmarks/`、`schemas/`、`examples/`、`tests/`、`deploy/`、`third_party/` 和 `docs/`；
- 架构/规范放 `docs/spec/`，模型交接放 `docs/handoffs/`，跨模型状态放 `docs/coordination/`，图形源文件放 `docs/diagrams/`；
- 测试代码、公开 fixtures 和 OpenGuard-Bench 分别放 `tests/`、`tests/fixtures/`、`benchmarks/`，不得混放到业务代码目录；
- 禁止用 `final`、`new`、`copy`、`临时` 等含糊名称制造重复版本；版本通过 Git、契约版本和 ADR 管理；
- 本地缓存和隔离环境必须由 `.gitignore` 排除；任务结束时复核目录和 `git status`，不清理用户文件，不把本机临时产物纳入竞赛仓库。
